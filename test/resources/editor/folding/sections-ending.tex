\begin{document}<fold text='...'>
    <fold text='foldme'>%! region foldme
    <fold text='\subsection{two}...'>\subsection{two}
    Subsection should be folded.</fold>
    %! endregion</fold>

    <fold text='\section{Only}...'>\section{Only}
    Fold this, since it is a section.</fold>
</fold>\end{document}
